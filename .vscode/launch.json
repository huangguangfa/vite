{
    "version": "0.2.0",
    "sourceMaps": true,
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:6888",
            "webRoot": "${workspaceFolder}"
        }
    ]
}